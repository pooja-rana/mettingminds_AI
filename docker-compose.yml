version: "3.9"
services:
  meetingmind:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:///./meetingmind.db
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    volumes:
      - .:/app
